# Client Instructions

- Before finishing work in `client/`, run and pass:
  - `bun run test`
  - `bun run lint`
  - `bun run format`
  - `bun run check`
- Do not consider client work complete if any of those commands fail.
