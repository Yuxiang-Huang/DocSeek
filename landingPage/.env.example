# Base URL of the DocSeek web app (no trailing path). CTAs use this; if unset, the
# landing page defaults to https://docseek.up.railway.app
# VITE_DOCSEEK_APP_URL=https://docseek.up.railway.app

# For local DocSeek client instead of Railway:
# VITE_DOCSEEK_APP_URL=http://localhost:5173
